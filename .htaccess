# Enable URL Rewriting
RewriteEngine On

# Remove .html extension
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^([^\.]+)$ $1.html [NC,L]

# Remove .htm extension
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^([^\.]+)$ $1.htm [NC,L]

# Redirect from .html to clean URL
RewriteCond %{THE_REQUEST} ^GET\ /(.+)\.html\ HTTP
RewriteRule (.+)\.html$ /$1 [R=301,L]

# Redirect from .htm to clean URL
RewriteCond %{THE_REQUEST} ^GET\ /(.+)\.htm\ HTTP
RewriteRule (.+)\.htm$ /$1 [R=301,L]

# Set index.html as default
DirectoryIndex index.html home.htm

# Optional: Force HTTPS (uncomment if you want)
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_PATH} [L,R=301]
